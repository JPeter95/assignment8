<!DOCTYPE html>
<html>
  <head>
    <title>Assignment8</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag '//ajax.googleapis.com/ajax/libs/jqueryui/1.12.1/themes/mint-choc/jquery-ui.min.css' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if logged_in? %>
      <div style='float: right;'>
        <%= link_to "Home", todo_lists_path, method: :get %> |
        <span>
          You are logged in as: <b>
          <%= link_to "#{ current_user.login }", accounts_path, method: :get %></b> |
        </span> 
        <%= link_to "Logout", logout_path, method: :delete %>
      </div>
    <% end %>

    <% flash.each do |key, value| %>
      <%= content_tag :p, value, id: key %>
    <% end %>

    <%= yield %>
  </body>
</html>
